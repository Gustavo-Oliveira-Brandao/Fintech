package com.fiap.fintech.repositories;

import com.fiap.fintech.models.Meta;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MetaRepository extends JpaRepository<Meta, Long> {
}
